# turtle_question_judge_v1

将 `dist/Workflow-turtle_question_judge_v1-draft.zip` 直接导入扣子，选择可用模型后发布，
再把工作流 ID 写入 `COZE_QUESTION_JUDGE_WORKFLOW_ID`。输入为冻结题目的 `surface`、
`bottom`、`language`、`key_points` 与 `player_question`。工作流只返回四类判定、展示文案、
命中点 key 和安全说明；普通回复不得泄露汤底。
