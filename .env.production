# 生产环境变量
# 在生产构建时使用 (npm run build)

# H5 默认使用当前页面域名下的 /api/v1 与 /ws/，无需绑定构建时域名。
# 非 H5 平台可在构建环境中注入绝对地址：
# VITE_API_BASE_URL=https://example.com/api/v1
# VITE_WS_BASE_URL=wss://example.com/ws/

# 环境名称
VITE_ENV_NAME=production
